const prodBuilder = require('./prod-builder.js');
const devBuilder = require('./dev-builder.js');
const testBuilder = require('./test-builder');

module.exports = {
    prodBuilder,
    devBuilder,
    testBuilder,
};